export type Produto = {
    id: string;
    nome: string;
    descricao: string;
    preco: string;
    quantidade: string;
    imagem: string;
}

export type Usuario = {
    id: string;
    nome: string;
    email: string;
    senha: string;
}